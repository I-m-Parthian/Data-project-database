-- Q. Write another SQL script that cleans up the user, and database created in the previous step.

-- Run these scripts inside the sql prompt with any user
-- having privilage of CREATEDB AND CREATE ROLE

Drop DATABASE ipl;
DROP USER parth;